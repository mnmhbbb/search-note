import React from 'react';
import Layout from '../../layouts/Layout';

const Home = (props) => {
  console.log(props);
  return (
    <Layout>
      <h1>여기는 홈</h1>
    </Layout>
  );
};

export default Home;
